CREATE TABLE Student (
	Student_ID INT AUTO_INCREMENT,
    Student_Name VARCHAR(20) NOT NULL DEFAULT 'John Doe',
    Student_Major VARCHAR(20) NOT NULL DEFAULT 'Undecided',
    PRIMARY KEY (Student_ID)
);