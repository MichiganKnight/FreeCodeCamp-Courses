@using Sports_API.Areas.CFBScraper.Models
@model List<Sports_API.Areas.CFBScraper.Models.MultiTeamItem>
@{
    ViewData["Title"] = "College Football Teams";
}

<div class="container text-center">
    <h2>College Football Teams</h2>
    
    <table class="table table-striped table-bordered table-hover" style="width: 100%;">
        <thead>
            <tr>
                <th>Team Name</th>
            </tr>
        </thead>
        <tbody>
            @if (Model is { Count: > 0 })
            {
                @foreach (MultiTeamItem team in Model)
                {
                    <tr>
                        <td>
                            <a asp-area="CFBScraper" asp-controller="CFBTeam" asp-action="Team" asp-route-team="@team.DisplayName" asp-route-abbr="@team.Abbreviation" style="color: #@team.Color">@team.DisplayName</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td class="text-muted">No Teams Found</td>
                </tr>
            }
        </tbody>
    </table>
</div>